;; -*- mode: lisp;-*-
(pkg numbers/main)

(import fmt)
(import strconv)

(define (plus x y)
  (+ (x : int) (y : int)))

(define plus-1
  ((plus 1) : (int -> int)))

(define (main)
  (fmt.Println (strconv.Itoa (plus-1 (plus-1 10)))))

